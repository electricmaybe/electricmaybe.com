{%- comment -%}
  Single source of truth for "length" option handle aliases (language-agnostic).
  Used by product--variants (slider vs radios) and product--length-option-index (form price-per-meter).
  Add new locale handles here when needed (e.g. ,länge, for German).
{%- endcomment -%},length,cable-length,lengde,
